# Magic 8 Ball – ESP32

An LLM-powered Magic 8 Ball running on the **Waveshare ESP32-S3-Touch-AMOLED-1.75C** development board. Tap the screen or shake the device to receive a mystical answer, generated by a free LLM API and displayed inside a classic blue-triangle window on the 466×466 AMOLED display.

When offline or without an API key, it falls back to the original 20 Magic 8 Ball responses.

## Hardware

| Component | Detail |
|-----------|--------|
| MCU | ESP32-S3 (240 MHz, PSRAM) |
| Display | CO5300 466×466 QSPI AMOLED |
| Touch | CST9217 capacitive |
| IMU | QMI8658 6-axis (shake detection) |
| PMIC | AXP2101 (battery, charging) |

## Features

- **LLM-powered answers** – sends a prompt to any OpenAI-compatible API; defaults to [Groq](https://groq.com/) free tier
- **Classic fallback** – all 20 original Magic 8 Ball responses when offline
- **Shake to ask** – built-in accelerometer detects shaking
- **Tap to ask** – touch the ball on screen
- **Animated UI** – fade-out/fade-in transitions, thinking dots
- **WiFi config portal** – captive portal AP for first-time setup
- **Long-press reconfigure** – hold the screen 3 seconds to re-enter setup

## Getting Started

### Prerequisites

- [PlatformIO](https://platformio.org/) (CLI or VS Code extension)
- USB-C cable connected to the Waveshare board
- A free API key from [Groq](https://console.groq.com/) (or any OpenAI-compatible provider)

### First Boot

1. **Build and flash:**
   ```
   pio run -e esp32s3 --target upload --upload-port COM3
   ```

2. **Connect to the setup AP:**
   The display will show `Connect WiFi: Magic8Ball-Setup`. Join that WiFi network on your phone or laptop.

3. **Open the config portal:**
   Navigate to `http://192.168.4.1` in a browser. Enter your WiFi credentials, paste your Groq API key, and hit **Save & Connect**.

4. **Ask away:**
   After rebooting, tap the ball or shake the device to get a mystical AI-generated answer.

### Serial Monitor

```
pio device monitor -b 115200
```

## Project Structure

```
├── include/
│   ├── pin_config.h        # GPIO pin definitions
│   ├── lv_conf.h           # LVGL configuration
│   ├── magic8ball_ui.h     # UI API
│   ├── llm_client.h        # LLM API client
│   └── config_portal.h     # WiFi config portal
├── src/
│   ├── main.cpp            # Entry point, hardware init, main loop
│   ├── magic8ball_ui.c     # LVGL Magic 8 Ball interface
│   ├── llm_client.cpp      # Async LLM API client (FreeRTOS task)
│   └── config_portal.cpp   # WiFi AP + web config server
├── platformio.ini
└── README.md
```

## Configuration

Settings are stored in NVS flash and persist across reboots.

| Setting | Default | Description |
|---------|---------|-------------|
| WiFi SSID | *(none)* | Your WiFi network name |
| WiFi Password | *(none)* | WiFi password |
| API URL | `https://api.groq.com/openai/v1` | OpenAI-compatible endpoint |
| API Key | *(none)* | Bearer token for the LLM API |
| Model | `llama-3.3-70b-versatile` | Model identifier |

To reconfigure at any time, **long-press** the screen for 3 seconds to re-enter AP setup mode.

## Supported LLM Providers

Any service with an OpenAI-compatible `/chat/completions` endpoint works:

| Provider | Free Tier | API URL |
|----------|-----------|---------|
| [Groq](https://groq.com/) | Yes | `https://api.groq.com/openai/v1` |
| [OpenRouter](https://openrouter.ai/) | Some models | `https://openrouter.ai/api/v1` |
| [Together AI](https://together.ai/) | Trial credits | `https://api.together.xyz/v1` |

## License

This project is licensed under the [GNU General Public License v2.0](LICENSE).
